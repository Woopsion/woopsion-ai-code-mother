spring:
  application:
    name: woopsion-ai-code-mother-backend
  # mysql
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ai_generate_code
    username: root
    password: jia19881221

server:
  port: 8123
  servlet:
    context-path: /api

# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan: com.woopsion.woopsionaicodemother.controller
# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn
